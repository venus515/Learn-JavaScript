const name="venus"
const repoCount=50

console.log(name+repoCount+"value"); // this is outdated

console.log(`Hello my name is ${name}. and my repo count is ${repoCount}`); // modern way
const gameNew= new String('venusgarg')
console.log(gameNew.length); // o/p: 5
console.log(gameNew.toUpperCase()); //o/p:VENUS
const newString =gameNew.substring(0,3)
console.log(newString);
const anotherString=gameNew.slice(-9,4)
console.log(anotherString);